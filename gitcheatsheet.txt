# add files to stage area
git add  .

# see which state files are in 
git status

# save changes in .git direcotry /local
git commit -m "some msg"

# 4 areas in git
# localfolder/workingtree, staging, localrepo/localgitfolder, remote/online git repo/folder
